/**
 * @fileoverview Action Schemas — PLACEHOLDER (Phase 6)
 *
 * Defines user interactive actions (row actions, bulk actions).
 * Currently empty — placeholder for future implementation.
 *
 * @see docs/06-unused-and-gaps.md#32-actionschemasjsx
 * @module ConfigData/ActionSchemas
 */

// Phase 6: Kept as empty export to avoid breaking spread in DataPages.jsx
// When implementing, add action definitions like:
// rowActions: [{ key: "edit", label: "Edit", icon: IconEdit }]
// bulkActions: [{ key: "delete", label: "Delete Selected", icon: IconTrash }]
export const VendorsActions = {
    rowActions: [],
    bulkActions: []
};
