/**
 * Action Schemas
 * 
 * Defines user interactive actions.
 * 
 * 1. Row Actions: Three-dot menu or buttons in each row (Edit, Delete, View).
 * 2. Bulk Actions: Actions applied to selected rows (Delete Selected, Approve Selected).
 * 
 * Future Implementation:
 * Will support "Slide Down" actions with embedded forms or confirmation dialogs.
 */
export const VendorsActions = {
    // Placeholder for row actions (Edit, Delete) and bulk actions
    rowActions: [],
    bulkActions: []
};
