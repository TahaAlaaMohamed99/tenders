/**
 * Filter Schemas
 * 
 * Defines the "Advanced Side Filter" configuration.
 * These filters appear in a side popup or drawer, allowing complex queries
 * (e.g., multi-select ranges, date ranges, cross-field logic).
 * 
 * Future Implementation:
 * Will accept an array of filter definitions (e.g., { field, type: 'range', label }).
 */
export const VendorsFilter = {
    // Placeholder for future side-popup filter configuration
    filters: []
};
